import React from 'react';

// This component seems to have been superseded by WebNavigatorFlow.
// Keeping a valid placeholder to prevent build errors if the file is still referenced.
const LinkAnalyzerFlow: React.FC = () => {
    return (
        <div className="flex items-center justify-center h-full">
            <p className="text-text-secondary">This component is deprecated. Please use Web Navigator.</p>
        </div>
    );
};

export default LinkAnalyzerFlow;